language: ruby

services:
  - docker

before_install:
- docker build -t carlad/sinatra .
- docker run -d -p 127.0.0.1:3000:3000 carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec foreman start;"
- docker ps -a

script:
- bundle exec rake test